cmake_minimum_required(VERSION 3.16.0)
project(asd LANGUAGES CXX)
find_package(fmt QUIET)
if (TARGET fmt::fmt)
  message("ok")
else()
  message("not ok")
endif()
add_executable(asd main.cpp)
target_link_libraries(asd PRIVATE fmt::fmt)
