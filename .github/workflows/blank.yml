name: test

on:
  workflow_dispatch:
    inputs:
      is_draft:
        description: 'Actions run ID which to get artifacts from'
        required: true
        default: true
        type: boolean

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - asd: '["qw", "as", "zx"]'

    steps:
      - name: Run a multi-line script
        run: |
          echo "value: ${{ github.event.inputs.is_draft && 1 || 0 }}"
