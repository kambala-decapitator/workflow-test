name: windows
on:
  # push:
  #  branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - run: |
          (Get-Command curl -ErrorAction SilentlyContinue).Source -or (Get-Command wget -ErrorAction SilentlyContinue).Source -or "Neither curl nor wget is installed."
