name: windows
on:
  # push:
  #  branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    #defaults:
      #run:
        #shell: bash
    steps:
      # - run: where perl
        # shell: cmd
      - run: |
          winPerl=$(which -a perl | fgrep Strawberry)
          echo "$(dirname "$winPerl")" >> "$GITHUB_PATH"
        shell: bash
      - run: which -a perl
        shell: bash

      # - uses: actions/checkout@v4

      # - name: Upload artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: ffmpeg-build-dir
      #     compression-level: 0
      #     path: |
      #       ${{ env.CONAN_HOME }}/p/b/ffmp*/b/build-release
