name: windows
on:
  # push:
  #  branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-11-arm
    #defaults:
      #run:
        #shell: bash
    steps:
      - run: |
          pipx install conan
          conan profile detect
          conan install --requires sdl/2.32.2 -o 'sdl/*:sdl2main=True' -o 'sdl/*:shared=True'
        shell: bash

      # - uses: actions/checkout@v4

      # - name: Upload artifact
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: ffmpeg-build-dir
      #     compression-level: 0
      #     path: |
      #       ${{ env.CONAN_HOME }}/p/b/ffmp*/b/build-release
